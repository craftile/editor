---
'@craftile/core': minor
'@craftile/editor': minor
---

Add undoable `replaceRegion(regionId, structures)` support for replacing the root block content of an existing region while preserving region metadata.
