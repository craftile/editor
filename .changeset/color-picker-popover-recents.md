---
'@craftile/plugin-common-properties': patch
---

Move the color picker popover into the editor portal and only add recent colors after a completed color selection, instead of while sliders are actively moving.
